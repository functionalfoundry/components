import {
  Card,
  Image,
  Text,
  View,
} from '.'

test('Exports Card', () => {
  expect(Card).toBeDefined()
})

test('Exports Image', () => {
  expect(Image).toBeDefined()
})

test('Exports Text', () => {
  expect(Text).toBeDefined()
})

test('Exports View', () => {
  expect(View).toBeDefined()
})
