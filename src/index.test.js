import {
  Card,
  Text,
  View,
} from '.'

test('Exports Card', () => {
  expect(Card).toBeDefined()
})

test('Exports Text', () => {
  expect(Text).toBeDefined()
})

test('Exports View', () => {
  expect(View).toBeDefined()
})
